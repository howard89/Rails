<h1>All Tags</h1>
<ul id="tags">
    <% @tags.each do |tag| %>
        <li>
            <%= link_to tag.name, tag_path(tag) %>
        </li>
    <% end %>
</ul> 


<%= link_to "delete", tags_path(@tags), data: {confirm: "Really delete the tag?"}, method: :delete %>